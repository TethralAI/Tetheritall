apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName | default "gateway-sa" }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: gateway-read-config
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: gateway-read-config
subjects:
  - kind: ServiceAccount
    name: {{ .Values.serviceAccountName | default "gateway-sa" }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: gateway-read-config
