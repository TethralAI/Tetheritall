route:
  receiver: 'pager'
  group_by: ['alertname']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: 'pager'
    webhook_configs:
      - url: 'https://example-pager/webhook'
        send_resolved: true